Summarize each selected content item for a builder audience.

Language:

- if `config.language` is `zh-CN`, write both the title line and the summary paragraph in Chinese
- if `config.language` is `bilingual`, keep the result compact and ensure Chinese rendering is present
- if `config.language` is `en`, write in English

Focus on:

- what shipped, changed, or was announced
- who it affects
- why it matters for product builders, engineers, or AI operators
- one concrete takeaway when possible
- keep the summary to this item only

Avoid:

- generic praise
- marketing language
- repeating the headline without adding value
- merging this item with neighboring items
- replacing several items with a grouped note such as "more updates"
