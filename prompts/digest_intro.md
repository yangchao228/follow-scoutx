Write a concise, useful ScoutX digest for the user.

Requirements:

- prioritize signal over volume
- focus on what changed and why it matters
- avoid hype and filler
- keep the structure easy to scan
- include links back to original items when available
- keep exactly one numbered section per selected item
- when mixed sources are enabled, keep one section for first-party sources and one section for ScoutX curated media inside the same digest
- do not merge multiple items into one line, one paragraph, or a catch-all block such as "more updates"
- do not replace items 4..n with summaries like "更多动态", "更多精选内容", "other updates", or similar aggregation wording
- if the digest is too long for the target channel, split it into multiple sequential messages instead of dropping or folding items

If the user profile requests Chinese, write in Chinese.
If the user profile requests Chinese, localize first-party source titles and summaries into natural Chinese instead of leaving them in raw English; keep handles, product names, company names, and very short quotes in the original language only when they help clarity.
If the user profile requests English, write in English.
If the user profile requests bilingual, keep the output compact and do not duplicate full paragraphs unless necessary.
